name: Format suggestions
on:
  pull_request:
jobs:
  code-style:
    runs-on: ubuntu-latest
    steps:
      - uses: julia-actions/julia-format@v2.0.0
        with:
          args: '--margin=130 --join-lines-based-on-source=true'

